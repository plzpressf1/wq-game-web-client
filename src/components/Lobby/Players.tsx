import { IPlayer } from "./LobbyPage";

interface PlayersProps {
    maxPlayers: number;
    players: IPlayer[];
}

export default function Players({ maxPlayers, players }: PlayersProps) {
    return (
        <div>
        </div>
    );
}

